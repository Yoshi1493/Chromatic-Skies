using System.Collections;
using UnityEngine;
using static CoroutineHelper;

public class #SCRIPTNAME# : EnemyShooter<EnemyBullet>
{
    protected override IEnumerator Shoot()
    {
        yield return WaitForSeconds(ShootingCooldown);
    }
}